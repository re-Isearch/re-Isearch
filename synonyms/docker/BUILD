Build the Docker image
✅ CPU version:
docker compose build
or, if you prefer plain Docker:
docker build -t synonym-generator .
⚡ GPU version (requires NVIDIA GPU + drivers):
docker compose build
Make sure runtime: nvidia is enabled in docker-compose.yml.
Or with plain Docker + GPU support:
docker build -t synonym-generator-gpu .

